# YxStatus 自主可审计服务器监控系统 核心架构文档

> 版本: v1.1.0 (已完全闭环 GPT-6 Astra 架构终审 P0/P1 硬约束)  
> 工程目录: `/home/yasin/yx-status`  
> 核心定位: 无远程运维指令能力、最小权限运行、接入身份可验证、资源消耗有界、监测语义明确的自主可审计监控系统。  
> 视觉参考对标: https://mmwx.980815.xyz/

---

## 1. 架构原则与安全边界 (闭环 Astra P0 审查)

### 1.1 攻击面最小化准则
1. **纯只读采集**：
   - Agent 源码严禁导入 `os/exec`、`syscall.Exec` 及任何外部命令执行包；
   - 协议中无任何下发脚本、远程终端（WebSSH）、文件上传下载的数据结构；
   - **响应丢弃原则**：Agent 发送上报数据后，仅检查 HTTP 状态码是否为 `200` 或 `204`，严格限制响应读取上限为 1KB，**绝不解析任何服务端下发的 JSON/动态指令**，从根源上杜绝服务端失陷后反向控制受控机的可能性。
2. **最小权限运行 (Least Privilege)**：
   - Agent 统一以独立非特权用户 `yx-agent` 运行，严格限制在非 root 账户；
   - Linux 文件权限：配置文件 `0600`，工作目录 `0700`；
   - 网络 Ping 采用非特权无原始套接字模式（Unprivileged ICMP 借由 `net.ipv4.ping_group_range`，或回退至标准 TCP Connect 握手测速），不依赖 `CAP_NET_RAW`。
3. **单向出站通信 (Zero Inbound on Agents)**：
   - 所有受控节点仅主动向法国监控专机发起 HTTPS 出站连接；
   - 私网节点（Armbian 物理机、Spark 算力节点）无需任何端口映射或反向打洞。

### 1.2 认证与防重放机制 (HMAC-SHA256)
上报请求头规范：
- `X-Node-ID`: 节点唯一标识（如 `sg-oracle-prod`）
- `X-Timestamp`: Unix 时间戳秒数（如 `1773723000`）
- `X-Signature`: 签名串，计算方式：
  `Hex(HMAC_SHA256(NodeSecret, node_id + ":" + timestamp + ":" + SHA256(RequestBody)))`

服务端安全拦截校验：
1. **时钟窗口检查**：`abs(CurrentTime - X-Timestamp) <= 30s`，超出窗口直接拒收；
2. **防重放缓存**：服务端在内存维护短时 LRU 缓存（有效期 60 秒），记录已接收的 `(NodeID, Timestamp, Signature)`，已存在的签名直接判重并拒绝；
3. **签名恒定时间校验**：使用 `subtle.ConstantTimeCompare` 校验签名，防时序攻击。

---

## 2. 跨国网络测速矩阵 (Ping Matrix) 架构 (闭环 Astra P1 审查)

针对 8 台主机的真实网络拓扑，摒弃不切实际的“全量 8x8 直连”设想，采用**非对称分层测速模型**：

```
+-------------------------------------------------------------+
|              公网 6 节点互联互测环 (6x6 矩阵)                 |
|  新加坡 Oracle <---> 法国 Oracle <---> 韩国 Oracle           |
|         ^                 ^                 ^               |
|         |                 |                 |               |
|         v                 v                 v               |
|    日本 AWS    <--->   英国 AWS   <--->   美西 GCP           |
+------------------------------+------------------------------+
                               ^
                               | 单向出站链路质量探针 (仅作为 Source)
                               |
+------------------------------+------------------------------+
|            私网/NAT 2 节点 (Armbian 物理机 / Spark 算力机)     |
|  * 仅向公网 6 节点及同局域网对端发起出站 TCP-Ping / HTTP-RTT 探测 |
|  * 前端展板明确标识为 [NAT 出站探针]，不展示虚假的反向入站延迟   |
+-------------------------------------------------------------+
```

- **探测方式**：采用温和的 TCP Connect RTT 探测各目标主机的已知业务/SSH/Web 端口，每 30 秒轮询一次，单次握手超时设为 2 秒；
- **防风暴与防封禁**：每次探测只发 1 次 SYN 握手，禁止高频 ICMP Flood，严格规避 AWS/Oracle/GCP 云厂商安全机制告警。

---

## 3. 服务端高并发与广播防护 (闭环 Astra P0/P1 审查)

1. **慢客户端隔离机制 (Slow Consumer Protection)**：
   - WebSocket 客户端发送管道（`send chan []byte`）容量严格硬封顶为 `16`；
   - 服务端广播时执行非阻塞写入：
     ```go
     select {
     case client.send <- message:
     default:
         // 客户端积压，判定为慢客户端，主动关闭连接防内存泄露
         close(client.send)
         delete(hub.clients, client)
     }
     ```
   - 单台服务端最大并发 WebSocket 访客数硬封顶为 `2000`，超出拒绝升级，保护服务器资源。
2. **离线判断防抖状态机**：
   - 正常心跳周期：2 秒一次；
   - `Healthy`（健康）：上次上报时间 `< 10s`；
   - `Warning`（心跳延迟）：`10s <= 上报时间 < 30s`；
   - `Offline`（离线判定）：`上报时间 >= 30s`（连续 15 次重试失联才触发红灯警告，杜绝跨国海缆微抖动导致的频繁误报）。

---

## 4. 数据存储与无损恢复 (SQLite WAL)

1. **存储架构**：
   - 实时指标存入内存 `RingBuffer`（保留最近 60 个数据点，用于前端流速平滑曲线）；
   - 持久化采用 SQLite，启用 `PRAGMA journal_mode=WAL;` 与 `PRAGMA synchronous=NORMAL;`；
2. **流量与 SLA 恢复机制**：
   - 每 1 分钟执行一次轻量批量事务，将各节点的累计上行/下行字节基线与在线秒数写入 `node_stats_hourly` 表；
   - 服务端崩溃重启时，先读取最后一次落盘的累积数据，并与 Agent 首次上报的网卡原始值进行差值校准，实现流量历史与 SLA 的无缝拼接。

---

## 5. 前端视觉与交互规范 (对标 `https://mmwx.980815.xyz/`)

1. **界面层级**：
   - **全局统计条 (Top Summary)**：在网节点比率（如 `8/8 在线`）、全网实时总流速（上行/下行 Mbps）、总内存使用率、总 CPU 利用率；
   - **双模切换 (View Switcher)**：
     - **极客卡片流 (Card View)**：支持网格自适应，大卡片展示实时仪表与波形；
     - **高密数据表格 (Table View)**：紧凑行展示各节点 IP（已脱敏）、地区旗帜、核心负载、内存、硬盘与实时流速；
   - **跨国网络热力矩阵 (Ping Matrix)**：清晰展示各区域互连质量色阶图（翠绿/青蓝/琥珀黄/珊瑚红）。
2. **XSS 物理防御**：
   - 前端所有动态渲染文本（节点名、OS 描述、主机名）一律采用 `textContent` 或 Vue 原生插值 `{{ }}` 输出，严禁使用 `v-html` 或 `innerHTML`。
